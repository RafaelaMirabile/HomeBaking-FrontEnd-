export type registerUserInputs =
    {
        userEmail: string,
        passwd: string,
        userName: string
    }

export type LoginInputs =
    {
        userEmail: string,
        passwd: string,
    }